import React from "react";
import {Container} from "@material-ui/core";
import {Translation} from "../Translations";

export const Dashboard: React.FC<{t: Translation}> = (t) => {
    return <Container>
        Dashboard
    </Container>
}